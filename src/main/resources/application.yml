server:
  port: 8080
  servlet:
    context-path: /
    multipart:
      enabled: true
      max-file-size: 100MB
    encoding:
      charset: utf-8
      enabled: true

spring:
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher # swagger setting

  # H2
  datasource:
    driver-class-name: org.h2.Driver # org.h2.Driver, org.mariadb.jdbc.Driver
    url: jdbc:h2:tcp://localhost/~/test #jdbc:h2:tcp://localhost/~/test, jdbc:mariadb://IP:3306/portal?serverTimezone=Asia/Seoul
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect #org.hibernate.dialect.H2Dialect, org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  security:
    oauth2:
      client:
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

        registration:
          facebook:
            client-id: # 발급 받은 client-id #
            client-secret: # 발급 받은 client-secret #
            scope: # 필요한 권한 #

          google:
            client-id: # 발급 받은 client-id #
            client-secret: # 발급 받은 client-secret #
            scope: # 필요한 권한 #

          kakao:
            client-id: # 발급 받은 client-id #
            client-secret: # 발급 받은 client-secret #
            scope: # 필요한 권한 #
            redirect-uri: "{baseUrl}/{action}/oauth2/code/{registrationId}"
            authorization-grant-type: authorization_code
            client-name: kakao
            client-authentication-method: POST

          naver:
            client-id: # 발급 받은 client-id #
            client-secret: # 발급 받은 client-secret #
            scope: # 필요한 권한 #

# jwt
app:
  jwtSecret: vitasoftvitasoftvitasoftvitasoftvitasoftvitasoftvitasoftvitasoftvitasoftvitasoft # base64
  jwtExpirationMs: 3600000